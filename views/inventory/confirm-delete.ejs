<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="/css/confirm-delete.css">
</head>
<body>
    <div class="container">
        <h2>Confirm Deletion</h2>
        <p>Are you sure you want to delete <%= item.inv_make %> <%= item.inv_model %>? This action cannot be undone.</p>
        
        <form action="/inv/delete/<%= item.inv_id %>" method="POST">
            <button type="submit" class="confirm-button">Yes, Delete</button>
            <a href="/inv/list" class="cancel-button">Cancel</a>
        </form>
    </div>
</body>
</html>
